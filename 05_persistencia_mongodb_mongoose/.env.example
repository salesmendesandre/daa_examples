# Copia este fichero como .env y ajusta los valores a tu entorno.
# Si no se define MONGODB_URI, el proyecto usa por defecto un Mongo local
# en mongodb://localhost:27017/documind (ver src/config/database.ts).
MONGODB_URI=mongodb://localhost:27017/documind_examples
PORT=3005
